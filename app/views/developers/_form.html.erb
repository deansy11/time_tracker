<%= form_with(model: @developer, local: true) do |f| %>
  <% if @developer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@developer.errors.count, "error") %> prohibited this developer from being saved:</h2>

      <ul>
        <% @developer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, id: :developer_name %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.email_field :email, id: :developer_email %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit 'Create Developer' %>
  </div>
  <br>
<% end %>

<%= link_to 'Back', root_path %>
