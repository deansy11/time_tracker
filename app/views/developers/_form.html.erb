<h1>Developers Sign Up Here!</h1>

<%= form_with(model: @developer, local: true) do |f| %>
  <% if @developer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@developer.errors.count, "error") %> prohibited this developer from being saved:</h2>

      <ul>
        <% @developer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_for :developer, url: developers_path do |f| %>
  Name: <%= f.text_field :name, id: :developer_name %>
  Email: <%= f.text_field :email, id: :developer_email %>
  Password: <%= f.password_field :password %>
  Password Confirmation: <%= f.password_field :password_confirmation %>
  <%= f.submit "Sign me up" %>
<% end %>


<% end %>

<%= link_to 'Back', root_path %>
